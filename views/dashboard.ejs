<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Chama Yetu</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <%- include('partials/header') %>

  <div class="container mt-4">
    <h1 class="mb-4">Welcome to Chama Yetu Dashboard</h1>
    
    <!-- REAL STATS CARDS -->
    <div class="row g-4 mb-4">
      <div class="col-md-3">
        <div class="card text-white bg-primary h-100">
          <div class="card-body">
            <h5>Total Members</h5>
            <h2><%= stats.members %></h2>
            <small>Active members</small>
          </div>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card text-white bg-success h-100">
          <div class="card-body">
            <h5>Total Funds</h5>
            <h2>KES <%= stats.funds.toFixed(2) %></h2>
            <small>Combined contributions</small>
          </div>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card text-white bg-warning h-100">
          <div class="card-body">
            <h5>Active Loans</h5>
            <h2><%= stats.loans %></h2>
            <small>Pending repayment</small>
          </div>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card text-white bg-info h-100">
          <div class="card-body">
            <h5>Next Meeting</h5>
            <h6><%= stats.nextMeeting ? stats.nextMeeting.title : '-' %></h6>
            <small>
              <%= stats.nextMeeting ? new Date(stats.nextMeeting.date).toLocaleString() : 'Not scheduled' %>
            </small>
          </div>
        </div>
      </div>
    </div>

    <!-- Quick Actions -->
    <div class="row">
      <div class="col-md-12">
        <div class="card">
          <div class="card-header">
            <h5>Quick Actions</h5>
          </div>
          <div class="card-body">
            <div class="row text-center">
              <div class="col-md-3">
                <a href="/members/add" class="btn btn-primary w-100 py-3">
                  <h5>+ Add Member</h5>
                </a>
              </div>
              <div class="col-md-3">
                <a href="/contributions/record" class="btn btn-success w-100 py-3">
                  <h5>💰 Record Contribution</h5>
                </a>
              </div>
              <div class="col-md-3">
                <a href="/loans/pending" class="btn btn-warning w-100 py-3">
                  <h5>📋 Approve Loans</h5>
                </a>
              </div>
              <div class="col-md-3">
                <a href="/meetings/create" class="btn btn-info w-100 py-3">
                  <h5>📅 Schedule Meeting</h5>
                </a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
