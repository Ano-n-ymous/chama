<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Chama Yetu</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <%- include('../partials/header') %>

  <div class="container mt-4">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card shadow">
          <div class="card-header bg-success text-white">
            <h3>Mark Attendance (Meeting #<%= meetingId %>)</h3>
          </div>
          <div class="card-body p-4">
            <form method="POST" action="/meetings/attend/<%= meetingId %>">
              <div class="mb-3">
                <p class="text-muted">Check the box for members who attended:</p>
                
                <div class="list-group">
                  <% members.forEach(member => { %>
                    <label class="list-group-item d-flex justify-content-between align-items-center">
                      <span><%= member.name %></span>
                      <input type="checkbox" 
                             name="attendance[<%= member.id %>]" 
                             value="1"
                             <%= member.attended ? 'checked' : '' %>
                             class="form-check-input">
                    </label>
                  <% }); %>
                </div>
              </div>

              <button type="submit" class="btn btn-success">Save Attendance</button>
              <a href="/meetings" class="btn btn-secondary">Cancel</a>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
