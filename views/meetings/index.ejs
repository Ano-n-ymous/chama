<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Chama Yetu</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <%- include('../partials/header') %>

  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1>Meetings</h1>
      <% if (user.role === 'admin' || user.role === 'secretary') { %>
        <a href="/meetings/create" class="btn btn-primary">+ Schedule Meeting</a>
      <% } %>
    </div>

    <% if (meetings.length === 0) { %>
      <div class="alert alert-info">No meetings scheduled yet.</div>
    <% } else { %>
      <div class="row">
        <% meetings.forEach(meeting => { %>
          <div class="col-md-6 mb-3">
            <div class="card h-100">
              <div class="card-header bg-primary text-white">
                <h5><%= meeting.title %></h5>
              </div>
              <div class="card-body">
                <p><strong>Date:</strong> <%= new Date(meeting.date).toLocaleString() %></p>
                <p><strong>Agenda:</strong> <%= meeting.agenda %></p>
                <p><strong>Created by:</strong> <%= meeting.creator_name %></p>
                <div class="mt-3">
                  <a href="/meetings/attend/<%= meeting.id %>" class="btn btn-sm btn-success">Mark Attendance</a>
                </div>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
